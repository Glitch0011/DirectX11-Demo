WASD to move the camera.
Left-click to move the camera forwards.
Right-click on a vertex to pick it.
Q to move the camera along a spline.
E to move the lights.
Hold R to remove the environment map.
Hold T to remove the bump mapping.